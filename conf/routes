# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index

GET     /ws                         controllers.Application.ws
GET     /logging                    controllers.Application.startLogging
GET     /logging/:id                controllers.Application.retrieveData(id)
POST    /logging/:id                controllers.Application.logData(id)
POST    /hubnet                     controllers.Application.handleHubNet


# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
